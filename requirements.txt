streamlit>=1.28
numpy>=1.24
pandas>=2.0
scipy>=1.10
pulp>=2.7
cvxpy>=1.4
plotly>=5.17
# Tùy chọn (Bài 7 NSGA-II thật; nếu thiếu app tự fallback sang random-sampling):
pymoo>=0.6.1
# Tùy chọn (Bài 10 solver GLPK qua Pyomo; nếu thiếu app tự fallback sang linprog):
pyomo>=6.6
